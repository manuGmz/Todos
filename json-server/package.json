{
  "name": "json-server",
  "version": "1.0.0",
  "description": "mock server for learning purposes",
  "main": "index.js",
  "scripts": {
    "start-server": "json-server --watch db.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "manu",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
